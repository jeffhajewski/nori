[package]
name = "norikv-types"
version = "0.1.0"
edition = "2021"
publish = false
description = "Internal IDs, errors, small enums."
repository = "https://github.com/your-org/norikv"
readme = "README.md"

[dependencies]
nori-observe = { path = "../nori-observe" }
